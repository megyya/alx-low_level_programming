#include <stdio.h>

/**
 * main - Starting point of the program that uses printf and not puts
 *
 * Return: 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
