#include "main.h"
/**
 * _isdigit - function that checks for a digit (from 0 until 9)
 * @c: int to be checked
 * Return: 1 if c is a digit, else 0
 *
 */
int _isdigit(int c)
{
return (c >= '0' && c <= '9');
}

